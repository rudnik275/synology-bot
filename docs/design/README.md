# Design references

Visual direction for the Mini App is decided in [ADR 0006](../adr/0006-mini-app-design-system-neo-brutalism.md): **Neo-Brutalism**, single light mode, reusing the voice-clip design language.

- `downloads-mockup-neo-brutalism.html` / `.png` — build-target mockup of the **Downloads** tab (header health-chip, task cards with per-status badges + progress bars, Add FAB, 3-tab bottom nav). Static — animations are a first-class workstream per ADR 0006, not shown here. Open the HTML locally to inspect the exact tokens (colors, borders `3/5px`, hard offset shadows, Space Grotesk).
