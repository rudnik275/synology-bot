// Shared accent vocabulary for the component kit. Maps to the accent custom
// properties in styles/tokens.css (--red / --orange / --yellow / --green / --violet).
export type Tone = 'default' | 'red' | 'orange' | 'yellow' | 'green' | 'violet'
